# برومبت بدء شات جديد — نُسُك Survey

> انسخ كل ما تحت الخط والصقه كأول رسالة في الشات الجديد.

---

أنا محمد صفوت — المطوّر والمحلّل والكاتب التقني الوحيد لمشروع **نُسُك Survey**:
تطبيق ميداني لتتبّع وتركيب وإدارة **1,787 نقطة قارئ RFID** في المشاعر (منى ·
عرفات · مزدلفة · الجمرات) لموسم حج 1448هـ.

اشتغل معي مباشرة. **لا تسألني عن أي شيء في هذه الرسالة — كله مكتوب هنا.**

## كيف أعمل

- أكتب بالعامية المصرية. **ضع كل كلمة إنجليزية في سطر مستقل لوحدها** ثم أكمل
  العربي في سطر جديد — قاعدة صارمة لحل اتجاه النص المختلط.
- أفضّل التنفيذ على النقاش. شخّص السبب الجذري قبل أي إصلاح.
- ألتقط أعطال الواجهة من لقطات آيفون. أصف النتيجة المطلوبة وأترك لك التنفيذ،
  **لكني أعترض بوضوح لو الناتج غلط**.
- ردودي القصيرة («مفيش اكسبورت» · «ايه المنظر ده؟») تعني: شخّص وأصلح فورًا.
- **«مش عاوز كده تاني» = ابنِ فحصًا يمنع الرجوع، لا تَعِد.**
- كل القرارات المنتَجية لي، وكل التنفيذ لك.

## الوصول

```
المستودع : github.com/mhmdsfwt371/Project-survey   (عام)
الموقع   : mhmdsfwt371.github.io/Project-survey/
النسخة   : V14.66
بيئة العمل: /home/claude/app   (استنسخه أول الشات)
Firebase : project-survey-60600   (Auth + Firestore · خطة Spark)
```

**التوكن**: ابحث في محادثاتي السابقة عن `github_pat` أو `claude-nusuk-deploy`
واستخرجه بنفسك. **لا تسألني عنه.**

## الملفات

```
index.html          ~1م — التطبيق كله في ملف واحد (وهذا مقصود)
v14/index.html      نسخة مطابقة تُحدَّث في كل دفعة
sw.js               عامل الخدمة
manifest.json + manifest.webmanifest
firestore.rules     35 مجموعة
season1447.json     1,787 سجل × 14 حقلًا من الموسم الماضي
legacy/v13.99.html  النظام القديم — المرجع الدائم
docs/system.md      سجل الإصدارات: كل إصدار وسببه
docs/followup-template.xlsx   قالب متابعة المشروع (بلا ضغط عمدًا)
docs/api-schema.json · docs/parity.json · docs/afaqy-api.md
scripts/            15 جردًا (تحت)
```

## دورة الإصدار — إلزامية

1. رقّم في خمسة: `index.html` · `sw.js` · `docs/api-schema.json` ·
   `docs/system.md` · ملفَّي `.docx`
   **الترقيم في أول ظهور فقط (position < 120,000)** — الاستبدال الشامل كسر
   إصدارًا لأن تعليقات قديمة أعمق في الملف تحمل أرقامًا سابقة.
2. `npm run preflight` — يقرأ خطواته وبيئته من `docs-check.yml` نفسه
3. ادفع برسالة عربية تشرح **لماذا** لا ماذا

## شبكة الحراسة — 15 جردًا

```
check-version.mjs   الحارس (NUSUK_SKIP_AUDITS=1 في السحابة)
audit-data.mjs      47 فحصًا: تعريفات مكرَّرة · مصطلح موحَّد · لوحات
                    · متغيّرات نمط · شيفرة ميتة · أرقام محفورة
audit-zero.mjs      الحالة الصفرية: كل رقم معروض له مصدر
audit-review.mjs    57 فحصًا بعيون 8 أدوار في شركة
audit-crud.mjs      38 فحصًا: كل إضافة وتعديل وحذف تُجرَّب
audit-flow.mjs      43 خطوة: الدورة الحياتية كاملة
audit-guards.mjs    18 طفرة مزروعة يجب أن تُمسَك
audit-writes · audit-roles · audit-calc · audit-exports
audit-rules · audit-db-rules · audit-capacity · audit-scale
qa.mjs · parity.mjs · preflight.mjs
```

## قواعد التطوير — مستخلَصة من أعطال حقيقية

**الترقيع**
- كل تعديل: `Python rep(src, old, new)` مع `assert count==1` قبل كل استبدال
- بعده `node --check` على كل كتلة سكربت
- **إذا فشل استبدال في منتصف دفعة، لا شيء يُكتَب** — طبّق واحدًا واحدًا
- البحث في `index.html` بـ`sed` بمدى أسطر، لا `grep` واسع — 1,787 سجلًّا
  و SheetJS المصغَّر يُغرقان النتائج

**البناء**
- الأزرار تُوصَل في **التهيئة**، لا داخل الرسم — تكرّر وصار قاعدة
- `overflow-wrap` لا يُعمَّم — يكسر عرض الأرقام في الخلايا
- **كل قائمة طويلة فيها بحث**، وكل بحث يُعيد التركيز لموضع المؤشّر
- **كل لوح: رأس ثابت فيه إغلاق · جسم يُمرَّر وحده · ذيل ثابت** — محروس بفحص
- **كل شاشة إعدادات: إضافة وتعديل وحذف** — لا استثناء

**البيانات**
- **لا رقم يُكتَب بيد**: كل عدد يُشتقّ من مصدره. `audit-zero` يمنع
- **لا تركيبة لا وجود لها**: لا جمرات في عرفات — الصفر لا يعني «بعد»
- **العدد الذي لم يقع أساسه لا يُقدَّر**: التهيئة تتبع المسح، والتركيب المسح،
  والفك التركيب
- ارجع دائمًا لـ`legacy/v13.99.html` قبل بناء أي شيء
- الشركات قائمة مُدارة لا نص حر · الشاخص `^\d+/\d+$` صارمًا
- **الاصطلاح**: `signpost` للشاخص · `block` للمربع · «ہٹانا» للفك بالأردو

**المعمار**
- عامل الخدمة من الشبكة لا الكاش
- الصور في مجموعة `photos` منفصلة (حدّ الميغابايت)
- مزامنة تفاضلية لا قراءة كاملة
- **كل ما يُكتَب له مجموعة مسمّاة** في `col` — وإلا سقط في `misc` المغلقة
  ورُفض صامتًا (وقع مرتين: `sites` و`ships`)
- **الدور يُشتقّ من الوظيفة**، والقواعد تعرف 7 أدوار فقط: `engineer` ·
  `supervisor` · `tech` · `cprep` · `casm` · `cins` · `viewer` (+`admin`)
- CFG مصدر واحد للقيم · ذاكرة الإحصاء تُبطَل عند الكتابة

**اللغة**
- ثلاث لغات: عربي · إنجليزي · أردو — **كل نص جديد يُترجَم في نفس الدفعة**
- `AlignmentType.START` في `docx` مع `bidirectional:true` (لا `RIGHT`)
- «مسح نهائي» عبارة أمان تُكتَب عربية حرفًا بحرف — لا تُترجَم

## ما بُني (V14.1 → V14.66)

**الميدان**: خريطة Leaflet بعلامات مشكَّلة · مسح · تركيب · فك · إسناد ·
موقع جديد · عناوين الشبكة (546 نقطة) · تفاصيل النقطة بـ21 صفًّا من بيانة
1447

**الفرق**: 42 فنيًّا حقيقيًّا · فرق · 12 وظيفة بأدوار مشتقّة · حسابات ·
سجل نقلات · أرصدة

**السيارات**: أسطول بلوحات ورخص وتكلفة · أنواع بسعاتها · إسناد لشخص أو فريق
بنوع العمل · **سجل عهدة يُسأل بتاريخ** (للمخالفات) · ملاءمة السيارة لعملها

**التوريد**: شحنات برقم وصنف ومورّد وتاريخ شحن **ووصول متوقَّع** · حساب
التأخّر · إشعار عاجل مرة في اليوم

**الإدارة**: 30 شاشة إعدادات · صلاحيات من مصدرها (7×11) · تصعيد · RACI ·
دروس مستفادة · احتفاظ · سجل تدقيق حيّ

**التقارير**: 38 ورقة إكسل · KMZ · PDF · **تصدير ملف المتابعة بقالبه**
(58 صفًّا يُحدَّث، والقالب يبقى بايتًا ببايت)

**الإشعارات**: كل شخص يرى ما وُجِّه إليه أو إلى فريقه أو للجميع

## المعلَّق

1. **`BACKUP_KEY`** في GitHub Secrets — النسخ الاحتياطي مبني ولا يعمل بدونه
2. **69 زرًّا بلا خاصية** في 20 شاشة («حفظ المواعيد» · «حفظ المصفوفة» ·
   «نشر على Firestore» · «توليد» · أزرار إكسل وPDF في شاشات التقارير)
3. **37 `catch` فارغة** — فُرزت في V14.42، الباقي فحص توفّر مشروع
4. مستودع خاص للنسخ · إزالة ملفات توقيع أندرويد من المستودع العام

## أول ما تبدأ

```bash
git clone https://x-access-token:$TOKEN@github.com/mhmdsfwt371/Project-survey.git /home/claude/app
cd /home/claude/app && ln -sf /home/claude/node_modules node_modules
node scripts/audit-zero.mjs     # تأكّد أن الأرقام كلها لها مصادر
```

ثم قل لي: **جاهز — إيه اللي نبدأ بيه؟**
