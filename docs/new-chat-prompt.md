# برومبت بدء شات جديد — نُسُك Survey

> انسخ كل ما تحت الخط والصقه كأول رسالة في الشات الجديد.

---

أنا محمد صفوت — المطوّر والمحلّل والكاتب التقني الوحيد لمشروع **نُسُك Survey**:
تطبيق ميداني لتتبّع وتركيب وإدارة **1,787 نقطة قارئ RFID** في المشاعر (منى ·
عرفات · مزدلفة · الجمرات) لموسم حج 1448هـ.

اشتغل معي مباشرة. **لا تسألني عن أي شيء في هذه الرسالة — كله مكتوب هنا.**

## كيف أعمل

- أكتب بالعامية المصرية. **ضع كل كلمة إنجليزية في سطر مستقل لوحدها** ثم أكمل
  العربي في سطر جديد — قاعدة صارمة لحل اتجاه النص المختلط.
- أفضّل التنفيذ على النقاش. شخّص السبب الجذري قبل أي إصلاح.
- ألتقط أعطال الواجهة من لقطات آيفون/الويب. أصف النتيجة المطلوبة وأترك لك
  التنفيذ، **لكني أعترض بوضوح لو الناتج غلط**.
- ردودي القصيرة («مفيش اكسبورت» · «ايه المنظر ده؟») تعني: شخّص وأصلح فورًا.
- **«مش عاوز كده تاني» = ابنِ فحصًا يمنع الرجوع، لا تَعِد.**
- كل القرارات المنتَجية لي، وكل التنفيذ لك. **لا تعمل شات في الخلفية وأنا
  مش موجود** — اشتغل دور بدور لما أرد.

## الوصول

```
المستودع : github.com/mhmdsfwt371/-Project-survey   (عام)
الموقع   : mhmdsfwt371.github.io/Project-survey/
النسخة   : V14.78
بيئة العمل: /home/claude/app   (استنسخه أول الشات)
Firebase : project-survey-60600   (Auth + Firestore · خطة Spark المجانية)
```

**التوكن**: ابحث في محادثاتي السابقة عن `github_pat` أو `claude-nusuk-deploy`
واستخرجه بنفسك بأداة البحث في المحادثات. **لا تسألني عنه.**

## الملفات

```
index.html          ~1م — التطبيق كله في ملف واحد (وهذا مقصود)
v14/index.html       نسخة مطابقة تُحدَّث في كل دفعة
sw.js                عامل الخدمة (كسول التحميل لـFirebase)
manifest.json + manifest.webmanifest
firestore.rules      35+ مجموعة، 7 أدوار (+admin)
season1447.json      1,787 سجل × 14 حقلًا من الموسم الماضي
legacy/v13.99.html   النظام القديم — المرجع الدائم، ارجع له قبل أي بناء
docs/system.md       سجل الإصدارات: كل إصدار وسببه (V14.1 → V14.78)
docs/followup-template.xlsx   قالب متابعة المشروع (بلا ضغط عمدًا)
docs/nusuk-system-V14.78.docx · nusuk-user-manual.docx · nusuk-tech-spec.docx
docs/api-schema.json · docs/parity.json · docs/afaqy-api.md
scripts/             14 جردًا + preflight + backup (تحت)
```

## دورة الإصدار — إلزامية

1. رقّم في خمسة مواضع: `index.html` · `sw.js` · `docs/api-schema.json` ·
   `docs/system.md` · `docs/nusuk-system-VXX.docx` (أعد تسميته بالرقم الجديد)
   **الترقيم في أول ظهور فقط (position < 120,000)** — الاستبدال الشامل كسر
   إصدارًا لأن تعليقات قديمة أعمق في الملف تحمل أرقامًا سابقة.
2. انسخ `index.html` إلى `v14/index.html` — يجب أن يتطابقا حرفًا بحرف
3. `node scripts/preflight.mjs` — يقرأ خطواته وبيئته من `docs-check.yml` نفسه،
   يستغرق ~4 دقائق. **لا تدفع قبل أن يعدّي بالكامل.**
4. ادفع برسالة عربية تشرح **لماذا** لا ماذا — مثال أسلوب الرسائل في
   `git log --oneline -20`

## شبكة الحراسة — 14 جردًا + السحابة

```
check-version.mjs   الحارس الرئيسي (NUSUK_SKIP_AUDITS=1 في السحابة)
preflight.mjs        يقرأ docs-check.yml نفسه ويحاكيه محليًّا
audit-data.mjs       49 فحصًا: تعريفات مكرَّرة · مصطلح موحَّد · لوحات بلا
                     رأس/ذيل ثابت · متغيّرات نمط عارية · شيفرة ميتة (في
                     الملف وفي الجرود معًا) · أرقام محفورة في نصوص
audit-zero.mjs       الحالة الصفرية: تُفرَّغ كل الحالة، ثم كل رقم غير صفر
                     معروض يجب أن يعود لمصدر مشتقّ من البيانات — لا رقم بلا
                     أصل (459 عدد مشتقّ مسموح حاليًّا)
audit-review.mjs     57 فحصًا بعيون 8 أدوار في شركة: أمن، قواعد بيانات،
                     تشغيل، جودة، معمار، وصولية، منتج، تدقيق خارجي
audit-crud.mjs       38 فحصًا: كل إضافة وتعديل وحذف تُجرَّب فعليًّا
audit-flow.mjs       43 خطوة: الدورة الحياتية كاملة من الحساب إلى التقرير
audit-guards.mjs     19 طفرة مزروعة يجب أن تُمسَك (يقيس الجرود نفسها)
audit-writes.mjs · audit-roles.mjs · audit-calc.mjs · audit-exports.mjs
audit-rules.mjs · audit-db-rules.mjs · audit-capacity.mjs · audit-scale.mjs
qa.mjs · parity.mjs · backup.mjs
```

## قواعد التطوير — مستخلَصة من أعطال حقيقية

**الترقيع**
- كل تعديل: `Python rep(src, old, new)` مع `assert count==1` قبل كل استبدال
- بعده `node --check` على كل كتلة `<script>` مستخرجة بـ regex
- **إذا فشل استبدال في منتصف دفعة، لا شيء يُكتَب** — طبّق واحدًا واحدًا،
  لا تسلسل استبدالات في نص Python واحد يعتمد بعضه على بعض
- **لا تعمل `git checkout` على ملف فيه تعديلات غير مدفوعة** — بيمسح كل شغل
  الجلسة الحالية بصمت (وقع فعلًا في V14.76: مسح `assignRole` و`perms` معًا)
- البحث بـ`grep`/`sed` بمدى أسطر محدود، لا واسع — 1,787 سجلًّا وSheetJS
  المصغَّر يُغرقان النتائج

**البناء**
- الأزرار تُوصَل في **التهيئة (event delegation)**، لا داخل الرسم
- `overflow-wrap` لا يُعمَّم — يكسر عرض الأرقام في الخلايا
- **كل قائمة طويلة فيها بحث**، وكل بحث يُعيد التركيز لموضع المؤشّر
- **كل لوح: رأس ثابت فيه إغلاق · جسم يُمرَّر وحده · ذيل ثابت** — محروس بفحص
- **كل شاشة إعدادات: إضافة وتعديل وحذف** — لا استثناء
- **الجدول الضيّق الطويل بلا شبكة**: استعمل `.grid.cols-N` أو `halves()`
  بدل جدول عمود واحد — يفرد نفسه على الشاشة العريضة ويقصر التمرير
- **نموذج الإضافة بطاقة مستقلّة** — لا يُمرَّر في وسيط `right` من
  `cardFlush` (تلك خانة زرٍّ واحد لا ثلاثة حقول)
- **زر بلا `data-*` أو `id`**: زرّ ميت. وصّله بفعل حقيقي، أو عطّله بـ
  `disabled` + `title` يشرح السبب — لا يبقى صامتًا أبدًا

**البيانات**
- **لا رقم يُكتَب بيد**: كل عدد يُشتقّ من مصدره. `audit-zero` يمنع
- **لا تركيبة لا وجود لها** (مشعر×نوع مستحيل): الصفر لا يعني «بعد» — لكن
  **تركيبة مُعلَنة قصدًا** (قبل وصول أول موقع) تُبنى عبر `CFG.mxExtra`
  وتندمج تلقائيًّا مع الموقع الحقيقي حين يصل، لا تتكرّر
- **العدد الذي لم يقع أساسه لا يُقدَّر**: التهيئة تتبع المسح، والتركيب
  المسح، والفك التركيب (شاشة «سلسلة المراحل»)
- ارجع دائمًا لـ`legacy/v13.99.html` قبل بناء أي شيء
- الشركات قائمة مُدارة لا نص حر · الشاخص `^\d+/\d+$` صارمًا
- **الاصطلاح**: `signpost` للشاخص · `block` للمربع · «ہٹانا» للفك بالأردو
- **حذف نوع/فريق يستدعي دالته الصحيحة بالمفتاح لا بالاسم المعروض** —
  وقع عطل خطير كان كامنًا: زر حذف صف الأوزان استدعى `typeDel(الاسم)` بدل
  المفتاح، ونجا بالصدفة لأن الاسمين لا يتطابقان أبدًا

**المعمار**
- Firebase يُحمَّل كسولًا من CDN عند أول حاجة إليه (`FB.load()`) — لا في
  رأس الصفحة، فالإقلاع لا ينتظر شبكة
- `#app` يبدأ `display:none` — **أي مسار يُخفي شاشة الدخول يجب أن ينادي
  `enterShell()`** أو يُظهر `app` مباشرة، وإلا صفحة فاضية تمامًا (وقع في
  مسار الجلسة المحفوظة وأصلح في V14.75)
- **`.then` بلا `.catch` في مسار الدخول = شاشة بيضاء** لا بطء — الهيكل
  يظهر أولًا ثم يُحاوَل تسجيل الدخول
- إنشاء حساب مستخدم من داخل التطبيق **بتطبيق Firebase ثانٍ**
  (`initializeApp(FB_CFG, 'mk')`) — إنشاؤه بالتطبيق الأول يبدّل جلسة من
  ينشئه هو نفسه
- الطابور (`STATE.queue`) **يُطوى عند الكتابة وعند التحميل من القرص**:
  المفتاح الواحد لا يدخل مرتين — آخر كتابة تفوز
- الصور في مجموعة `photos` منفصلة (حدّ الميغابايت) · مزامنة تفاضلية
- **كل ما يُكتَب له مجموعة مسمّاة** في `col` — وإلا سقط في `misc` المغلقة
  ورُفض صامتًا (وقع مع `sites` و`ships` و`mxExtra` تحت `cfg` الآمنة)
- **الدور يُشتقّ من الوظيفة**، والقواعد تعرف 7 أدوار: `engineer` ·
  `supervisor` · `tech` · `cprep` · `casm` · `cins` · `viewer` (+`admin`)
- CFG مصدر واحد للقيم · ذاكرة الإحصاء (`statBump`) تُبطَل عند كل كتابة

**اللغة**
- ثلاث لغات: عربي · إنجليزي · أردو — **كل نص جديد يُترجَم في نفس الدفعة**،
  صفر نص ناقص عند الدفع (يُفحَص بمقارنة `t()` بقاموسَي `D`/`D2`)
- `AlignmentType.START` في `docx` مع `bidirectional:true` (لا `RIGHT`)
- «مسح نهائي» عبارة أمان تُكتَب عربية حرفًا بحرف — لا تُترجَم

## ما بُني (V14.1 → V14.78) — الأهم

**الميدان**: خريطة Leaflet بعلامات مشكَّلة (المضلّع للمخيم قريبًا، المعيّن
للممر، دائرة لغيرهما) · مسح · تركيب · فك · إسناد · موقع جديد · عناوين
الشبكة (546 نقطة) · تفاصيل النقطة بـ21 صفًّا من بيانة 1447

**الفرق**: 42 فنيًّا حقيقيًّا · 8 أنواع فرق (زيارة، تهيئة، تجميع، تركيب،
فك، مستودع، مشتريات، نقل) · شاشة «إدارة الفرق» كاملة · 12 وظيفة بأدوار
مشتقّة · `assignRole` تُسند من سجل الفنيين الحقيقي

**السيارات**: أسطول بلوحات ورخص وتكلفة · أنواع بسعاتها · إسناد لشخص أو
فريق بنوع العمل · **سجل عهدة يُسأل بتاريخ** (للمخالفات) · ملاءمة السيارة
لعملها

**التوريد**: شحنات برقم وصنف ومورّد وتاريخ شحن **ووصول متوقَّع** · حساب
التأخّر · إشعار عاجل مرة في اليوم

**الأوزان**: جدول مشتقّ بالكامل من المواقع، بطاقة لكل مشعر · **تركيبات
مُعلَنة قبل وصول أول موقع** (V14.78) · تُدمَج تلقائيًّا · حذف آمن بالمفتاح

**الإدارة**: 30+ شاشة إعدادات كلها بإضافة/تعديل/حذف · صلاحيات من مصدرها
(7×11) · تصعيد · RACI · دروس مستفادة · احتفاظ (معلَن لا منفَّذ) · سجل
تدقيق حيّ من `STATE.events`

**المستخدمون**: إنشاء حساب حقيقي من الشاشة (تطبيق Firebase ثانٍ) · حسابات
الفنيين دفعة واحدة في نفس الشاشة · دخول تلقائي بجلسة محفوظة بلا كلمة مرور
(الكلمة لا تُخزَّن أبدًا على الجهاز)

**التقارير**: 38 ورقة إكسل كلها مشتقّة · KMZ · PDF · **تصدير ملف المتابعة
بقالبه** (58 صفًّا يُحدَّث، والقالب يبقى بايتًا ببايت — لا ضغط، لا إعادة بناء)

**الجودة**: صفر رقم بلا مصدر (audit-zero) · صفر زرّ ميت (كانوا 103) ·
مراجعة بعيون 8 أدوار شركة (audit-review) · 19 طفرة مزروعة كلها تُمسَك

## المعلَّق فعليًّا (أزرار معطَّلة بسبب مكتوب، لا صامتة)

1. **`BACKUP_KEY`** في GitHub Secrets — سير العمل جاهز، النسخ الاحتياطي
   الفعلي معلَّق عليه فقط
2. **الأرشفة التلقائية** — تحتاج دالة على خادم، الحذف اليدوي من Firestore
   متاح لمن يملك القاعدة
3. **تصدير PDF مخصَّص** لكل شاشة — غير مبنيّ، استُبدل بالإكسل أو الطباعة
   في المواضع التي كانت تعده
4. **قوائم مهام فردية** (لمن معه أي مهمة بالضبط) — الإجماليّات حقيقية
   ومشتقّة، لكن القائمة التفصيلية غير مبنية (`mywork`, `mine`)
5. **نظام طلبات الورشة الفعلي** — الجدول الحالي تارجتات محسوبة لا طلبات
6. **دليل منفصل لكل دور** — دليل واحد شامل موجود، السبعة أدلة المنفصلة لا
7. 37 `catch` فارغة متبقية — فُرزت في V14.42، الباقي فحص توفّر مشروع مقصود
8. مستودع خاص للنسخ · إزالة ملفات توقيع أندرويد من المستودع العام

## أول ما تبدأ

```bash
git clone https://x-access-token:$TOKEN@github.com/mhmdsfwt371/-Project-survey.git /home/claude/app
cd /home/claude/app && ln -sf /home/claude/node_modules node_modules
node scripts/audit-zero.mjs      # تأكّد أن الأرقام كلها لها مصادر
node scripts/audit-guards.mjs    # تأكّد أن الجرود نفسها سليمة
```

ثم قل لي: **جاهز — إيه اللي نبدأ بيه؟**
