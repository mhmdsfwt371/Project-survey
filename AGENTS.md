# قارئات أفاقي — دستور العمل للوكلاء

> يقرؤه كودكس تلقائيًّا، ويستورده كلود كود من `CLAUDE.md`. القواعد هنا نافذةٌ على كلِّ وكيلٍ يعمل على المستودع، وما يخالفها تُسقطه البوابةُ في السحابة قبل أن يصل الإنتاج. يحرس هذا الملفَّ نفسَه `scripts/audit-agents.mjs`.

## ١. المشروع
نظامٌ يتابع مشروعَ قارئات RFID لحج ١٤٤٨هـ من المسح الميداني حتى التركيب والفكّ وتسليم الأجهزة للوزارة. تطبيقُ ويبٍ يعمل بلا شبكة في ملفٍّ واحد، وقاعدة Firestore، ونشرٌ على GitHub Pages.

| الملف | ما فيه |
|---|---|
| `index.html` | التطبيقُ كلُّه: كتلةُ سكربتٍ واحدة، والتنسيق، وقاموسا الترجمة `D` و`D2` |
| `sw.js` | عاملُ الخدمة (التخزين بلا شبكة) |
| `scripts/audit-*.mjs` | الجرودُ الدائمة — أكثرُ من مئة |
| `scripts/check-version.mjs` | الحارسُ الكامل |
| `.github/workflows/docs-check.yml` | قائمةُ الجرود التي تشغّلها السحابة — المرجعُ الواحد |
| `docs/system.md` | القواعدُ الحاكمة (§١٤) وسجلُّ الإصدارات (§١٦) — اقرأ أحدثَ صفوفه قبل العمل |

## ٢. البيئة — إلزامية
```bash
npm i --no-save --no-audit --no-fund jsdom@^24 docx@^9 jszip@^3
```
لا تثبّت حزمةً أخرى داخل المستودع: `audit-deps` يُسقط الحارسَ إن حمل الجهازُ مكتبةً فوق قائمة السحابة. Node 20.

## ٣. الفروع والدفع
- **لا يُدفَع إلى `main` مباشرةً.** الدفعُ إلى `staging` بعد نجاح الحارس الكامل محلّيًّا:
  ```bash
  node scripts/check-version.mjs      # نحو ١٢ دقيقة — لا تكتم مخرجاته
  git push origin HEAD:staging
  ```
- السحابةُ تشغّل القائمةَ في أربع شرائح، ثم المتصفّحَ الحقيقيَّ ومحاكيَ القواعد، وتكتب حالةَ الالتزام `nusuk/gate`. **الترقيةُ إلى `main` تركب القطار** (V17.97): `train.yml` الأحدَ والأربعاءَ ٠١:٠٠ UTC يرقّي آخرَ التزامٍ خضراءَ بوابتُه، أو تشغيلٌ يدويٌّ له، أو التزامٌ يحمل الترويسةَ `Release: hotfix` على سطرٍ وحدَه فيُرقَّى فورًا — للطوارئ وحدَها، لا للدفعات العادية. لا يُرقَّى أحمرُ قطّ.
- الفحصُ السريعُ للتطوير فقط: `NUSUK_SKIP_AUDITS=1 node scripts/check-version.mjs` و`node scripts/quick.mjs <أسماء الجرود>`.
- **وكيلان لا يعملان على الموضوع نفسه ولا على `index.html` في الوقت نفسه.** كلُّ وكيلٍ على فرعه (`claude/<مهمة>` أو `codex/<مهمة>`) ويُدمَج عبر `staging`.

## ٤. دورةُ الإصدار — كلُّ تغييرٍ نسخة
الرقمُ يرتفع مع كلِّ تغيير (`Vx.y` → `Vx.y+1`) في المواضع كلِّها معًا:
1. وسمُ الرأس في `index.html`: `>نسخة Vx.y</button>` داخل `class="ver-tag"` — **لا** «أوّلُ ظهورٍ للرقم في الملف» (التعليقاتُ تحمل أرقامًا قديمة).
2. `sw.js`: `const CACHE = 'nusuk-survey-vx.y';`
3. `docs/api-schema.json`: `app_version` + مدخلٌ في `changelog`.
4. `docs/system.md`: سطرُ «آخر تحديث» + صفٌّ في جدول §١٦ (الأحدثُ أوّلًا).
5. `docs/nusuk-system-Vx.y.docx` (يُعاد تسميتُه ويُبدَّل الرقمُ داخل `word/document.xml`) و`docs/nusuk-user-manual.docx`.
6. `node scripts/role-manuals.mjs` — أدلةُ الأدوار الأربعةَ عشر.
7. مدخلٌ في `RELEASE_NOTES` داخل `index.html`:
   - تغييرٌ يراه المستخدم: سطورٌ **بعربيةٍ فصيحةٍ مبسَّطةٍ بلا تشكيلٍ ولا عامّيةٍ ولا مصطلحاتٍ داخلية** — يفهمها ممثّلُ الوزارة من أوّل قراءة.
   - نسخةُ بنيةٍ لا يراها المستخدم: `notes:[]`.

## ٥. قواعدُ الشيفرة — تُسقطها الجرود
- **الترجمة:** كلُّ `t('…')` مفتاحُه في قاموسي الإنجليزية والأردية. أضِف بعد المرساتين `'اسمٌ مركَّب':'Compound names',` و`'اسمٌ مركَّب':'مرکب نام',`. المفتاحُ يُكتَب بالحرف لا بـ`\uXXXX`.
- **لا خاصيةَ `data-*` تُرسَم بلا معالجِ ضغط** (`audit-buttons`). للعلامات غير التفاعلية: `id` أو `aria-*`.
- **لا دالةَ تُعرَّف مرتين**، ولا رقمَ نسخةٍ حرفيًّا في الشيفرة خارج `RELEASE_NOTES`.
- **الاسمُ في الواجهة «قارئات أفاقي».** لا «نُسُك» بالتشكيل في أيِّ واجهة.
- **الأرقامُ تعريفٌ واحد:** الحالةُ من `lifeOf`، والإحصاءُ من `siteKeyStats`، والوصولُ من `svReached` / `svStuck` / `svDone`. لا تحسب حالةً بتعريفٍ ثانٍ — `audit-reconcile` يطابق الأرقامَ في تسع شاشات.
- **ألوانُ ما تراه الوزارة** من متغيّرات `--min-*` في `:root` وحدَها.
- **خياراتُ `<select>`**: القيمةُ المفتاحُ العربيُّ، والمعروضُ `t(…)`.
- **لا أسرارَ في المستودع** (عام). التوكناتُ في أسرار GitHub وحدَها.
- التعليقاتُ عربيةٌ تشرح «لماذا»، ورسائلُ الالتزام عربيةٌ وصفية.

## ٦. كلُّ ميزةٍ بجردها
كلُّ ميزةٍ يصحبها جردٌ دائمٌ `scripts/audit-<اسم>.mjs` بفحوصٍ على بياناتٍ معلومةِ الجواب، ويُسجَّل في `docs-check.yml` **قبل** السطر:

`# ─── نهايةُ قائمة الحارس ───`

(ما بعد العلامة للسحابة وحدَها: الشرائحُ والترقية.)

## ٧. خريطةٌ سريعة
| ما تبحث عنه | أين |
|---|---|
| الأنواعُ والأشكال | `CAT_DEF`، `typesList()`، `SHAPES` |
| المشاعر | `zoneOptions()`، `zoneCode()` |
| دورةُ النقطة | `lifeOf()`، `LIFE`، `stuckList()` |
| الإحصاء | `siteKeyStats()`، `chainRows()` |
| شاشةُ الوزارة | `kioskBody()`، `kioskStory()`، `zoneForecast()`، `pointMap()`، `geoOutliers()`، `pmZoomAt()` |
| تفاصيلُ المسح | `svdRows()`، `SVD_CARDS` |
| الصفحاتُ والشرائح | `NAV`، `TABS`، `PAGE.<id>` |
| الثوابت | `CFG`، `cfgGet()`، `cfgInput()` — `settings/points` |
| ما الجديد | `RELEASE_NOTES`، `whatsNewMaybe()` |
| التحديات | `chalKey()`، `svdChals()`، `chalOtherCard()` |
| الاستعمال | `pageTick()`، `usageCard()`، `readsToday()` |
| الأداء والتشخيص | `perfSummary()`، `perfCard()`، `boxNote()`، `boxText()` |
| عزل النص | `bdi()`، `bdiText()`، `fsi()`، `fsiText()` — للعرض والرسائل لا للبيانات |
| الميدان | `sunSet()`، `wakeSync()`، `scanOpen()`، `serialOwner()`، `basemapDownload()`، `deviceCards()` |
| التصدير القياسي | `geoJsonBuild()` — والمخطّطُ من `scripts/schema-gen.mjs` |
| التوأم | `twinOf()`، `dupMerge()` |
| السيور | `season.yml` التغذية · `pulse.yml` النبض · `ministry.yml` الوزارة · `myafaqy.yml` الجسر · `backup.yml` النسخ |
