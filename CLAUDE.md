# قارئات أفاقي — تعليمات كلود كود

@AGENTS.md

## التعاونُ مع كودكس — إضافة `codex-plugin-cc`
- **كلود:** التخطيط، والتغييراتُ التي تمسُّ أكثرَ من موضع (الأرقام، دورةُ النقطة، التصميم)، والأعطالُ الصعبة، ومراجعةُ شغل كودكس قبل الدمج.
- **كودكس:** التنفيذُ المحدَّدُ المنفصل. سلّمه بـ`/codex:rescue` مع: الهدف، والملفّات، ومعاييرِ القبول، وأسماءِ الجرود التي يجب أن تمرّ.
- بعد أيِّ تغييرٍ كبيرٍ وقبل الحارس: `/codex:review`.
- **لا تفعّل** `--enable-review-gate` — حلقاتٌ تستنزف الحدود.
- مع صاحب المشروع: عاميةٌ مصرية، وأيُّ كلمةٍ لاتينيةٍ في سطرٍ مستقل.

## ترتيبُ الجلسة
1. `git pull` ثم اقرأ أحدثَ صفوف `docs/system.md` §١٦.
2. نفّذ ← جردُ الميزة ← ختمُ النسخة (`AGENTS.md` §٤) ← `NUSUK_SKIP_AUDITS=1 node scripts/check-version.mjs`.
3. `node scripts/check-version.mjs` كاملًا، ثم `git push origin HEAD:staging`.
4. تحقّق من الترقية: `git fetch && git log origin/main -1`.
